node_modules/
.env
.DS_Store
npm-debug.log*
*.log
data/app-data.json
data/*.local.json
# data/reddit-state.json + reddit-session-meta.json are intentionally committed
# when present (private internal repo) — Docker seeds the session from the repo.
# The old game-launch session was removed; create a new one for this tool's own
# Reddit account with `npm run reddit:login`, then commit it.
data/reddit-chrome-profile/
data/*.db
data/*.db-shm
data/*.db-wal
data/*.db-journal
data/*.bak
coverage/
.claude/
