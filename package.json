{
  "name": "launch-pilot-growth-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Steam game portfolio outreach, UTM, key distribution, CSV import, and API sync dashboard.",
  "scripts": {
    "start": "node src/server.mjs",
    "dev": "node --watch src/server.mjs",
    "discover": "node src/discovery/run.mjs",
    "reddit:login": "node src/redditSession.mjs login",
    "reddit:dump": "node src/redditSession.mjs dump",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "playwright": "^1.60.0"
  }
}
